name: Deploy to Netlify

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Cache Cypress binary
        uses: actions/cache@v4
        with:
          path: ~/.cache/Cypress
          key: cypress-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            cypress-${{ runner.os }}-

      - name: Install dependencies
        run: npm ci

      - name: Install Cypress binary
        run: npx cypress install

      - name: Run linter
        run: npm run lint

      - name: Build project
        run: npm run build

      - name: Run Cypress tests
        run: npx start-server-and-test "npm run preview -- --host 127.0.0.1 --port 4173" http://127.0.0.1:4173 "npm run cypress:run -- --config baseUrl=http://127.0.0.1:4173"

      - name: Deploy to Netlify
        run: npx --yes netlify-cli deploy --prod --dir=dist --site "$NETLIFY_SITE_ID"
        env:
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_PROJECT_ID }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        if: github.ref == 'refs/heads/main'

      - name: Deploy Preview to Netlify
        run: npx --yes netlify-cli deploy --dir=dist --site "$NETLIFY_SITE_ID"
        env:
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_PROJECT_ID }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        if: github.event_name == 'pull_request'
